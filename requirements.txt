Flask==3.0.3
Flask-PyMongo==2.3.0
pymongo==4.7.2
python-dotenv==1.0.1
Werkzeug==3.0.3
Flask-Login==0.6.3
Flask-SocketIO==5.3.6
simple-websocket==1.1.0
Pillow==10.4.0
opencv-python==4.10.0.84

# Optional sign-model training
pandas==2.2.3
scikit-learn==1.5.2
joblib==1.4.2
# Sign recognition runtime (install separately if your Python version supports it):
# mediapipe

# GCN + Transformer sign recognition (primary model)
torch>=2.5
# torch-geometric: install with the wheel matching your torch+CUDA version.
# CPU-only (works on any machine):
#   pip install torch-geometric
# With CUDA 12.x:
#   pip install torch-geometric torch-scatter torch-sparse -f https://data.pyg.org/whl/torch-2.5.0+cu121.html
# The runtime falls back to BiGRU/SVM if torch-geometric is unavailable.
torch-geometric

gunicorn==23.0.0
